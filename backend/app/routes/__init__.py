"""FastAPI route modules that make up the Vino API surface.

Each sibling module defines an ``APIRouter`` for one area of the API (blocks,
irrigation, insight, settings, etc.); ``app/main.py`` imports and mounts them.
"""
