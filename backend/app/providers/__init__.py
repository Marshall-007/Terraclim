"""Pluggable weather/climate data sources (Open-Meteo, TerraClim, curated data
pack, synthetic fixture) behind a common interface, disk cache, and
resilience wrapper. See `factory.py` for provider selection.
"""
